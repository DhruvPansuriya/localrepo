/*
Clone: cloning a repository on our local machine

   type this-> git clone <-some link-> to add the github file.
    // cd means change directory
   
   type-> git status  to find status.
   type-> git add filename  to make file staged.
   type-> git commit -m "message"  to make file committed.
   type-> git push origin main to upload our localrepo to github.
   type-> git init to make this folder a git repo.
 